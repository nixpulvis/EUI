local M, S, V = unpack(select(2, ...))
-----------------------------------------------------------------------
-- EUI /module
-----------------------------------------------------------------------
--[[ for enabling and disabling modules ]]

